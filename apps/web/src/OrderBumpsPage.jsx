import { useEffect, useMemo, useState } from 'react';
import { ArrowLeft, Check, ImageOff, LoaderCircle, PackagePlus, Plus, Search, Sparkles } from 'lucide-react';
import { createProduct, getCheckouts, getProducts, publishCheckout, updateCheckoutDraft, uploadProductImage } from './api';
import { defaultCheckoutConfig } from './CheckoutEditor';

const money = new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' });
const emptyEditor = { mode: 'existing', productId: '', title: '', message: '', productTitle: '', productPrice: '', compareAtPrice: '', productDescription: '', imageUrl: '' };
function ImageUpload({ value, onChange, csrfToken }) { const [busy, setBusy] = useState(false); const [error, setError] = useState(''); async function send(file) { if (!file) return; if (!['image/jpeg', 'image/png', 'image/webp'].includes(file.type)) { setError('Use uma imagem JPG, PNG ou WebP.'); return; } setBusy(true); setError(''); try { const result = await uploadProductImage(file, csrfToken); onChange(result.imageUrl); } catch (e) { setError(e.message || 'Não foi possível enviar a imagem.'); } finally { setBusy(false); } } return <div className="ob-image-upload" onDragOver={event => event.preventDefault()} onDrop={event => { event.preventDefault(); void send(event.dataTransfer.files?.[0]); }}>{value ? <img src={value} alt="Prévia do produto"/> : <ImageOff size={21}/>}<div><b>{busy ? 'Otimizando imagem...' : 'Clique para enviar uma imagem'}</b><small>ou arraste e solte aqui · JPG, PNG ou WebP</small><label><input type="file" accept="image/jpeg,image/png,image/webp" onChange={event => void send(event.target.files?.[0])}/>Selecionar imagem</label>{error && <em>{error}</em>}</div></div>; }

export default function OrderBumpsPage({ csrfToken }) {
  const [state, setState] = useState({ loading: true, checkouts: [], products: [], error: '' }); const [query, setQuery] = useState(''); const [filter, setFilter] = useState('all'); const [editor, setEditor] = useState(null); const [busy, setBusy] = useState(false); const [notice, setNotice] = useState('');
  async function load() { try { const [checkouts, products] = await Promise.all([getCheckouts(), getProducts({ status: 'active', pageSize: 100 })]); setState({ loading: false, checkouts: checkouts.items, products: products.items, error: '' }); } catch (e) { setState(current => ({ ...current, loading: false, error: e.message })); } }
  useEffect(() => { void load(); }, []);
  const checkout = state.checkouts.find(item => item.slug === 'principal') || state.checkouts[0];
  const bumpsOf = c => Array.isArray(c.draftConfig?.orderBumps) && c.draftConfig.orderBumps.length ? c.draftConfig.orderBumps : c.draftConfig?.orderBumpProductId ? [{ productId: c.draftConfig.orderBumpProductId, title: c.draftConfig.orderBumpTitle || '', message: c.draftConfig.orderBumpMessage || '' }] : [];
  const items = useMemo(() => state.checkouts.flatMap(c => bumpsOf(c).map((bump, index) => ({ checkout: c, bump, index, product: state.products.find(p => p.publicId === bump.productId) })).filter(item => item.product)).filter(item => `${item.product.checkoutTitle} ${item.bump.title} ${item.checkout.name}`.toLowerCase().includes(query.toLowerCase())).filter(item => filter === 'all' || (filter === 'active' ? item.checkout.status === 'PUBLISHED' : item.checkout.status !== 'PUBLISHED')), [state, query, filter]);
  function open(item) { const bump = item?.bump; setEditor({ ...emptyEditor, mode: 'existing', editingProductId: bump?.productId || '', productId: bump?.productId || '', title: bump?.title || '', message: bump?.message || '' }); setNotice(''); }
  async function save() { if (!checkout) { setNotice('Crie um checkout principal antes de criar uma oferta.'); return; } setBusy(true); try { let productId = editor.productId; if (editor.mode === 'independent') { const priceCents = Math.round(Number(editor.productPrice.replace(',', '.')) * 100); const compareAtCents = editor.compareAtPrice ? Math.round(Number(editor.compareAtPrice.replace(',', '.')) * 100) : undefined; if (!editor.productTitle.trim() || !Number.isInteger(priceCents) || priceCents < 1 || (compareAtCents !== undefined && (!Number.isInteger(compareAtCents) || compareAtCents <= priceCents))) throw new Error('Informe nome, preço da oferta e, se usar, um preço original maior.'); const created = await createProduct({ title: editor.productTitle.trim(), description: editor.productDescription.trim() || undefined, imageUrl: editor.imageUrl || undefined, priceCents, compareAtCents, trackInventory: false, maxPerOrder: 1, active: true }, csrfToken); productId = created.product.publicId; } if (!productId) throw new Error('Selecione ou crie um produto para a oferta.'); const oldBumps = bumpsOf(checkout).filter(bump => bump.productId !== editor.editingProductId); if (oldBumps.some(bump => bump.productId === productId)) throw new Error('Este produto já está configurado como order bump.'); const orderBumps = [...oldBumps, { productId, title: editor.title.trim(), message: editor.message.trim() }]; const first = orderBumps[0] || {}; const config = { ...defaultCheckoutConfig, ...(checkout.draftConfig || {}), showBump: true, orderBumps, orderBumpProductId: first.productId || '', orderBumpTitle: first.title || '', orderBumpMessage: first.message || '' }; await updateCheckoutDraft(checkout.publicId, config, csrfToken); await publishCheckout(checkout.publicId, csrfToken); setEditor(null); await load(); } catch (e) { setNotice(e.message); } finally { setBusy(false); } }
  async function removeBump() { if (!checkout || !editor?.editingProductId || !window.confirm('Remover este order bump do checkout? O produto será preservado.')) return; setBusy(true); try { const orderBumps = bumpsOf(checkout).filter(bump => bump.productId !== editor.editingProductId); const first = orderBumps[0] || {}; await updateCheckoutDraft(checkout.publicId, { ...defaultCheckoutConfig, ...(checkout.draftConfig || {}), showBump: orderBumps.length > 0, orderBumps, orderBumpProductId: first.productId || '', orderBumpTitle: first.title || '', orderBumpMessage: first.message || '' }, csrfToken); await publishCheckout(checkout.publicId, csrfToken); setEditor(null); await load(); } catch (e) { setNotice(e.message); } finally { setBusy(false); } }
  const selected = state.products.find(p => p.publicId === editor?.productId);
  if (state.loading) return <main className="page"><section className="card products-state"><LoaderCircle className="spin"/><span>Carregando order bumps...</span></section></main>;
  if (editor) return <main className="order-bump-builder"><header><button className="icon-btn" onClick={() => setEditor(null)} aria-label="Voltar"><ArrowLeft size={19}/></button><b>{selected ? 'Editar order bump' : 'Criar order bump'}</b><div><button className="secondary" onClick={() => setEditor(null)}>Cancelar</button><button className="primary" disabled={busy} onClick={() => void save()}>{busy ? <LoaderCircle className="spin" size={16}/> : <Check size={16}/>} Salvar order bump</button></div></header><div className="ob-builder-grid"><section className="ob-form"><div className="ob-tabs"><button className="active">Oferta</button><button type="button" disabled>Aparência</button></div><div className="ob-form-body"><h2>Informações do order bump</h2><label className="ob-toggle">Order bump ativo<input type="checkbox" checked readOnly/><span/></label><div className="ob-mode"><button className={editor.mode === 'existing' ? 'selected' : ''} onClick={() => setEditor(current => ({ ...current, mode: 'existing' }))}>Produto existente</button><button className={editor.mode === 'independent' ? 'selected' : ''} onClick={() => setEditor(current => ({ ...current, mode: 'independent', productId: '' }))}>Produto independente</button></div><label>Título da oferta<input value={editor.title} maxLength="120" onChange={e => setEditor(current => ({ ...current, title: e.target.value }))} placeholder="Ex.: Você ganhou uma oferta especial!"/></label><label>Mensagem complementar<textarea value={editor.message} maxLength="300" onChange={e => setEditor(current => ({ ...current, message: e.target.value }))} placeholder="Ex.: Adicione ao pedido antes de finalizar."/></label><hr/>{editor.mode === 'existing' ? <label>Produto e preço<select value={editor.productId} onChange={e => setEditor(current => ({ ...current, productId: e.target.value }))}><option value="">Selecione um produto...</option>{state.products.map(p => <option key={p.publicId} value={p.publicId}>{p.checkoutTitle} — {money.format(p.priceCents / 100)}</option>)}</select></label> : <><div className="ob-price-grid"><label>Preço da oferta<input value={editor.productPrice} inputMode="decimal" onChange={e => setEditor(current => ({ ...current, productPrice: e.target.value }))} placeholder="R$ 0,00"/></label><label>Preço original (opcional)<input value={editor.compareAtPrice} inputMode="decimal" onChange={e => setEditor(current => ({ ...current, compareAtPrice: e.target.value }))} placeholder="R$ 0,00"/></label></div><label>Nome do produto<input value={editor.productTitle} maxLength="240" onChange={e => setEditor(current => ({ ...current, productTitle: e.target.value }))} placeholder="Ex.: Aspirador Nasal Infantil"/></label><label>Descrição do produto<textarea value={editor.productDescription} onChange={e => setEditor(current => ({ ...current, productDescription: e.target.value }))} placeholder="Explique o que o cliente recebe"/></label><label>Imagem do produto<ImageUpload value={editor.imageUrl} onChange={value => setEditor(current => ({ ...current, imageUrl: value }))} csrfToken={csrfToken}/></label></>}{notice && <p className="public-error" role="alert">{notice}</p>}</div></section><section className="ob-preview"><span className="eyebrow">PRÉVIA NO CHECKOUT</span><div className="ob-preview-card"><span className="ob-preview-check"><Check size={14}/></span>{editor.mode === 'independent' && editor.imageUrl ? <img src={editor.imageUrl} alt=""/> : selected?.imageUrl ? <img src={selected.imageUrl} alt=""/> : <span className="ob-preview-image"><Sparkles size={20}/></span>}<div><b>{editor.title || 'Oferta especial'}</b><strong>{editor.mode === 'independent' ? (editor.productTitle || 'Seu produto complementar') : (selected?.checkoutTitle || 'Selecione um produto')}</strong><p>{editor.message || (editor.mode === 'independent' ? editor.productDescription : selected?.checkoutDescription) || 'Uma oferta complementar para seu pedido.'}</p></div><em>+ {editor.mode === 'independent' ? money.format(Number(editor.productPrice.replace(',', '.')) || 0) : selected ? money.format(selected.priceCents / 100) : 'R$ 0,00'}</em></div><small>O cliente pode aceitar ou recusar a oferta antes de pagar.</small></section></div></main>;
  return <main className="page order-bumps-page"><section className="page-title"><div><p className="eyebrow">GESTÃO</p><h1>Order bumps</h1><p>Ofertas complementares para aumentar seu ticket médio.</p></div><button className="primary" onClick={() => open()}><Plus size={17}/> Novo order bump</button></section><section className="card ob-list-card"><div className="ob-toolbar"><div className="product-search"><Search size={17}/><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Buscar por código ou nome..."/></div><div className="ob-filter">{[['all','Todos'],['active','Ativos'],['inactive','Inativos']].map(([value,label]) => <button className={filter === value ? 'active' : ''} key={value} onClick={() => setFilter(value)}>{label}</button>)}</div></div>{state.error && <p className="public-error" role="alert">{state.error}</p>}{items.length ? <div className="order-bump-list">{items.map(item => <button className="order-bump-card" key={`${item.checkout.publicId}-${item.bump.productId}`} onClick={() => open(item)}><div className="order-bump-thumb">{item.product.imageUrl ? <img src={item.product.imageUrl} alt=""/> : <ImageOff size={21}/>}</div><div><span className="eyebrow">{item.checkout.status === 'PUBLISHED' ? 'ATIVO' : 'RASCUNHO'}</span><h2>{item.bump.title || item.product.checkoutTitle}</h2><p>{item.bump.message || item.product.checkoutDescription || 'Oferta complementar'}</p></div><strong>{money.format(item.product.priceCents / 100)}</strong></button>)}</div> : <div className="ob-empty"><span><PackagePlus size={25}/></span><h2>Nenhum order bump cadastrado</h2><p>Crie seu primeiro order bump para aumentar suas vendas com ofertas adicionais.</p><button className="primary" onClick={() => open()}><Plus size={16}/> Criar order bump</button></div>}</section></main>;
}
