import React,{useEffect,useRef,useState}from'react';
import{Check,Copy,EllipsisVertical,ExternalLink,Link2,LoaderCircle,Plus,Send,Trash2,Webhook,X}from'lucide-react';
import{createStoreWebhook,deleteStoreWebhook,getStoreWebhooks,testStoreWebhook,updateStoreWebhook}from'./api';
import'./webhooks-page.css';
const eventOptions=[['order.created','Pedido criado','Quando um novo Pix é gerado'],['order.paid','Pedido pago','Quando o pagamento é confirmado'],['order.cancelled','Pedido cancelado','Quando um pedido é cancelado'],['order.refunded','Pedido estornado','Quando um pagamento é reembolsado'],['payment.failed','Pagamento falhou','Quando uma tentativa de pagamento falha']];
const blank={name:'',description:'',url:'',secret:'',active:true,events:[]};
function Dialog({initial,onClose,onSaved,csrfToken}){const ref=useRef(null);const[form,setForm]=useState(initial?{...initial,secret:''}:blank);const[busy,setBusy]=useState(false);const[error,setError]=useState('');const[revealed,setRevealed]=useState('');const toggle=event=>setForm(value=>({...value,events:value.events.includes(event)?value.events.filter(item=>item!==event):[...value.events,event]}));useEffect(()=>{const key=e=>e.key==='Escape'&&onClose();window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key);},[onClose]);const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{const result=initial?await updateStoreWebhook(initial.publicId,form,csrfToken):await createStoreWebhook(form,csrfToken);if(result.secret){setRevealed(result.secret);setBusy(false);}else onSaved();}catch(err){setError(err.message);setBusy(false);}};return <div className="webhook-overlay" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section ref={ref} className="webhook-dialog" role="dialog" aria-modal="true" aria-labelledby="webhook-dialog-title"><header><div><h2 id="webhook-dialog-title">{initial?'Editar webhook':'Novo webhook'}</h2><p>Receba notificações em tempo real dos eventos da sua loja.</p></div><label className="webhook-active"><input type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/><i/><span>Ativo</span></label><button onClick={onClose} aria-label="Fechar"><X/></button></header>{revealed?<div className="webhook-secret"><Check/><h3>Webhook criado</h3><p>Copie a chave secreta agora. Ela não será exibida novamente.</p><code>{revealed}</code><button className="secondary" onClick={()=>navigator.clipboard.writeText(revealed)}><Copy size={16}/> Copiar chave</button><button className="primary" onClick={onSaved}>Concluir</button></div>:<form onSubmit={submit}><fieldset><legend>Identificação</legend><p>Nome e descrição para organizar seus webhooks.</p><div className="webhook-two"><label>Nome<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Ex.: Integração ERP" required maxLength="120"/></label><label>Descrição<input value={form.description||''} onChange={e=>setForm({...form,description:e.target.value})} placeholder="Opcional" maxLength="240"/></label></div></fieldset><fieldset><legend>Endpoint</legend><p>URL pública de destino e chave para validar as requisições.</p><label>URL do webhook<div className="webhook-url"><Link2 size={17}/><input type="url" value={form.url} onChange={e=>setForm({...form,url:e.target.value})} placeholder="https://api.sualoja.com/webhooks" required/></div></label>{!initial&&<label>Chave secreta<input value={form.secret} onChange={e=>setForm({...form,secret:e.target.value})} placeholder="Opcional — gerada automaticamente" minLength="16"/></label>}</fieldset><fieldset><legend>Eventos <span>{form.events.length} selecionados</span></legend><p>Escolha quais notificações este webhook deve receber.</p><div className="webhook-events">{eventOptions.map(([id,title,description])=><label key={id}><input type="checkbox" checked={form.events.includes(id)} onChange={()=>toggle(id)}/><i/><span><b>{title}</b><small>{description}</small></span></label>)}</div></fieldset>{error&&<p className="webhook-error" role="alert">{error}</p>}<footer><button type="button" className="secondary" onClick={onClose}>Cancelar</button><button className="primary" disabled={busy||!form.name.trim()||!form.url||!form.events.length}>{busy?<LoaderCircle className="spin"/>:'Salvar webhook'}</button></footer></form>}</section></div>}
export default function WebhooksPage({storeKey,csrfToken}){const[data,setData]=useState({loading:true,error:'',items:[],writable:false});const[editing,setEditing]=useState(null);const[menu,setMenu]=useState(null);const[testing,setTesting]=useState('');const load=()=>{const controller=new AbortController();setData(v=>({...v,loading:true,error:''}));getStoreWebhooks(controller.signal).then(v=>setData({loading:false,error:'',...v})).catch(e=>e.name!=='AbortError'&&setData(v=>({...v,loading:false,error:e.message})));return controller;};useEffect(()=>{const controller=load();return()=>controller.abort();},[storeKey]);const remove=async item=>{if(!confirm(`Excluir o webhook “${item.name}”?`))return;await deleteStoreWebhook(item.publicId,csrfToken);setMenu(null);load();};const test=async item=>{setTesting(item.publicId);try{const result=await testStoreWebhook(item.publicId,item.events[0]||'order.created',csrfToken);alert(result.success?`Teste entregue com sucesso (HTTP ${result.statusCode}).`:`O endpoint respondeu HTTP ${result.statusCode}.`);load();}catch(e){alert(e.message);}finally{setTesting('');}};return <main className="page webhooks-page"><div className="webhook-breadcrumb">Sistema <span>›</span> Webhooks</div><section className="page-title"><div><h1>Webhooks</h1><p>Receba notificações em tempo real sobre eventos da sua loja.</p></div>{data.writable&&<button className="primary" onClick={()=>setEditing({})}><Plus/> Novo webhook</button>}</section>{data.loading?<div className="webhook-state"><LoaderCircle className="spin"/> Carregando webhooks...</div>:data.error?<div className="webhook-state error">{data.error}<button className="secondary" onClick={load}>Tentar novamente</button></div>:!data.items.length?<div className="card webhook-empty"><Webhook/><h2>Nenhum webhook configurado</h2><p>Conecte seu ERP, automação ou sistema interno aos eventos da SOLID.</p>{data.writable&&<button className="primary" onClick={()=>setEditing({})}><Plus/> Criar primeiro webhook</button>}</div>:<div className="webhook-grid">{data.items.map(item=><article className="card webhook-card" key={item.publicId}><header><h2>{item.name}</h2><span className={item.active?'active':'inactive'}>{item.active?'Ativo':'Inativo'}</span><button aria-label={`Ações de ${item.name}`} onClick={()=>setMenu(menu===item.publicId?null:item.publicId)}><EllipsisVertical/></button>{menu===item.publicId&&<div className="webhook-menu"><button onClick={()=>setEditing(item)}>Editar</button><button onClick={()=>test(item)} disabled={testing===item.publicId}>{testing===item.publicId?'Enviando...':'Enviar teste'}</button><button className="danger" onClick={()=>remove(item)}><Trash2/> Excluir</button></div>}</header><a href={item.url} target="_blank" rel="noreferrer"><Link2/>{item.url}<ExternalLink/></a>{item.description&&<p>{item.description}</p>}<div className="webhook-tags">{item.events.map(event=><span key={event}>{eventOptions.find(option=>option[0]===event)?.[1]||event}</span>)}</div><footer><span>{item._count.deliveries} envios</span>{item.deliveries[0]&&<span className={item.deliveries[0].success?'ok':'fail'}>{item.deliveries[0].success?'Último envio entregue':'Último envio falhou'}</span>}<button onClick={()=>test(item)} disabled={testing===item.publicId} aria-label="Enviar teste"><Send/></button></footer></article>)}</div>}{editing&&<Dialog initial={editing.publicId?editing:null} onClose={()=>setEditing(null)} onSaved={()=>{setEditing(null);load();}} csrfToken={csrfToken}/>}</main>}
