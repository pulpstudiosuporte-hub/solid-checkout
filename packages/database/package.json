{
  "name": "@solid/database", "version": "0.1.0", "private": true, "type": "module",
  "exports": { ".": { "types": "./src/index.ts", "default": "./dist/src/index.js" } },
  "scripts": {
    "generate": "prisma generate", "build": "npm run generate && tsc -p tsconfig.json", "typecheck": "npm run generate && tsc --noEmit",
    "db:migrate": "prisma migrate dev", "db:deploy": "prisma migrate deploy", "db:status": "prisma migrate status", "db:seed": "tsx prisma/seed.ts"
  },
  "dependencies": { "@prisma/adapter-pg": "7.9.1", "@prisma/client": "7.9.1", "pg": "8.23.0", "prisma": "7.9.1" }
}
